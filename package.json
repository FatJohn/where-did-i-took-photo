{
  "name": "where-did-i-took-photo",
  "private": true,
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "lint": "pnpm -r lint",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck",
    "build": "pnpm -r build"
  }
}
